# Generated by audit L1/L3 closure on 2026-05-10.
# Sourced from lib/features/training/presentation/training_strings.dart —
# that file is now a back-compat shim that delegates to
# AppLocalizations.of(context). Translations: en (canonical), uz, ko,
# ru, vi. Non-English copies are seeded with the English strings and
# marked with `// TODO: translate` so a translator can fill in later
# without blocking the wiring.

arb-dir: lib/l10n
template-arb-file: app_en.arb
output-localization-file: app_localizations.dart
output-class: AppLocalizations
output-dir: lib/l10n
preferred-supported-locales:
  - en
  - uz
  - ko
  - ru
  - vi
