name: hanguk_app
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.18+2031

environment:
  sdk: ^3.11.4

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  # Audit L1/L3: Material widget translations + the localizationsDelegates
  # composite + intl runtime for `AppLocalizations`. Required for the
  # generated training l10n files under lib/l10n/.
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.3.1
  freezed_annotation: ^3.1.0
  json_annotation: ^4.9.0
  go_router: ^17.1.0
  supabase_flutter: ^2.12.2
  file_picker: ^8.0.0
  image_picker: ^1.1.2
  webview_flutter: ^4.13.1
  url_launcher: ^6.3.0
  dio: ^5.9.2
  path_provider: ^2.1.5
  open_filex: ^4.7.0
  # Audit B7: vapi is vendored under ./packages/vapi (see overrides
  # below), pinning the wire format. Bumping requires regenerating
  # `vapi_event_parser.dart` + retesting the 4 known endCall event
  # shapes in test/features/training/vapi_event_parser_test.dart.
  vapi: any
  http: ^1.6.0
  webview_flutter_web: ^0.2.3+4
  table_calendar: ^3.2.0
  intl: ^0.20.2
  state_notifier: ^1.0.0
  package_info_plus: ^9.0.1
  permission_handler: ^11.4.0
  audioplayers: ^6.6.0
  # Auto-updater
  install_plugin: ^2.1.0      # one-tap APK install on Android via PackageInstaller.Session
  crypto: ^3.0.6              # SHA-256 verification of downloaded APK
  device_info_plus: ^11.5.0   # OS / model info for telemetry pings
  # Audit K10 (2026-05-11): pre-emptive add ahead of any future
  # Kakao Login (or other OAuth) flow вЂ” OAuth tokens cannot live
  # in SharedPreferences. Keychain (iOS) + EncryptedSharedPreferences
  # (Android). Not consumed by any code yet вЂ” see docs/runbooks/kakao.md.
  flutter_secure_storage: ^9.2.2

  # Crash reporting (P1 from store_readiness audit). DSN supplied via
  # --dart-define=SENTRY_DSN=...; empty DSN no-ops cleanly.
  sentry_flutter: ^8.0.0
  # Account export / share intent — used by /account export-my-data UI.
  share_plus: ^10.1.4
  cross_file: ^0.3.4+2
  # Play Store in-app update flow (P0 #1 split: store builds use this,
  # non-store builds keep the existing APK updater).
  in_app_update: ^4.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  build_runner: ^2.13.1
  freezed: ^3.2.5
  go_router_builder: ^4.2.1
  json_serializable: ^6.9.0
  flutter_launcher_icons: ^0.14.4

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  generate: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/virtual_tour/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  web:
    generate: true
    image_path: "assets/app_icon2.png"
    background_color: "#ffffff"
    theme_color: "#ffffff"
  image_path: "assets/app_icon2.png"
  min_sdk_android: 21

dependency_overrides:
  freezed_annotation: ^3.1.0
  vapi:
    path: ./packages/vapi
  # QA fix 2026-05-06: dart_jsonwebtoken 3.4.0 (transitive via gotrue/supabase_flutter)
  # references pointycastle 3.x APIs (ECPrivateKey, ASN1Parser, etc.) that were
  # removed in pointycastle 4.0.0. Pin pointycastle to the last 3.x line so the
  # Dart compiler can resolve those symbols.
  pointycastle: ^3.9.1
